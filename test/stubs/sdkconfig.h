#ifndef TEST_STUB_SDKCONFIG_H
#define TEST_STUB_SDKCONFIG_H

/* Host-test stand-in for the generated ESP-IDF sdkconfig.h. Intentionally
 * empty: host builds exercise the release-shaped configuration (no
 * CONFIG_BRAMBLE_OTA_ALLOW_HTTP). */

#endif
