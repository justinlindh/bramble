# Ed25519 per-node identity (campaign) progress ledger
Plan: ~/src/internal-planning/plans/2026-07-05-ed25519-identity-plan.md. Base main d699c273 (post 398bbdf3). Branch feat/ed25519-identity.
Decisions: D0 airtime=YES; D2 rebind address to Ed25519 (flag day); D3 ship phases 0-4, defer Sybil phase 5.
Primitive: device=espressif/libsodium, host=OpenSSL EVP_PKEY_ED25519. Keygen via entropy-gated crypto_random seed. Shared RFC8032 KAT+malleability vectors pin host==device.
CI: gitea-runner wedges -> docker restart + fresh push (runbooks/gitea-api.md).
Phases: 0 primitive | 1 keypair+NVS+address-rebind | 2 attestation frame | 3 verify+TOFU-pin | 4 gate timesync/DM on identity.
--
Phase 0 (primitive) DONE. See task-p0-report.md. crypto_ed25519_{keypair,sign,verify} + BRAMBLE_ED25519_{PUBKEY(32),SECKEY(64),SIG(64)}_SIZE in crypto.h; device=libsodium crypto_sign_* (new components/crypto/idf_component.yml espressif/libsodium ^1.0.20 -> 1.0.22~1, REQUIRES espressif__libsodium), host=OpenSSL EVP_PKEY_ED25519. sk format = libsodium seed||pk (host consumes seed half; tested invariant). Keygen seed via crypto_random -> crypto_entropy_fill(esp_random) gate, fail-closed scratch-buffer pattern mirroring crypto_generate_identity. test/test_ed25519.c: RFC8032 7.1 TEST 1/2/3/SHA(abc) exact-byte KAT + S+L malleation rejected + flipped-byte + roundtrip/wrong-key/tampered-msg (TDD red->green). 97/97 host, board build linked libsodium (symbol-level verified; crypto_ed25519_* GC'd until Phase 1 calls them), cppcheck+clang-format-v14(runner)+rpc-contract+gosim green. CARRY: small-order-point verdict divergence (libsodium rejects, OpenSSL accepts) documented; constrain Phase 3 verify semantics accordingly.
