# Flooding F1 (unicast flood transport) progress ledger
Design: ~/src/internal-planning/plans/2026-07-04-flooding-substrate-design.md
Plan: ~/src/internal-planning/plans/2026-07-04-flooding-F1-plan.md
Base main: 7cc27ca0 (post PR #125 tooling). Branch: feat/flooding-f1.
PIVOT: flooding substrate (owner-committed). Build unicast flood + flooded-ACK behind s_flood_transport toggle (default OFF, coexist w/ reactive). Prove firmware flood through gosim matches the MODEL (n=25 100/100, n=50 80/80, n=100 90/90). Reactive removal is F3.
Reuse Phase 1 channel_flood_decide + s_flood_dedup (suppress=2). Authenticated flood (data_auth_verify before relay). NO parallel impl. 
Verify per task: host suite + board build + cppcheck(runner) + clang-format(runner v14) + check-rpc-contract + gosim go test -count=1.

Task 1 (unicast DATA floods behind toggle): DONE. See task-1-report.md. s_flood_transport (NVS bramble_flood, RPC bramble.setFloodTransport, reflected in getConfig floodTransportEnabled). handle_data's broadcast flood-relay block generalized to also cover unicast-for-other under the toggle (dest_is_broadcast || (s_flood_transport && !dest_is_self)); same channel_flood_decide + s_flood_dedup, no second impl. mesh_process_rx_packet's DATA_RX_FORWARD branch picks handle_data (flood) vs forward_data_packet (reactive) by the toggle. gosim bridge.c mirrors via g_flood_transport_enabled + shared bridge_flood_relay() helper + scenario field "flood_transport". Fixed a pre-existing cross-test flake (sim_emitter g_emitter_quiet leaking from radioHarness tests into later Sim() tests within the same process) at the root in bridge_init(). All green: 93/93 host, rpc 52/52, cppcheck clean, board build clean, gosim go test -count=1 green x3. Concern flagged: firmware channel_flood_decide has no suppress=2 heard-count mechanism (only gosim's Go model does); pre-existing Phase 1 gap, not introduced/fixed here.
Task 1: DONE (commit 71aabcde, Sonnet). s_flood_transport toggle (NVS + bramble.setFloodTransport RPC + getConfig floodTransportEnabled + openapi). Unicast-for-others reuses channel_flood_decide via the generalized broadcast guard (dest_broadcast || (flood && !self)) + early-return so relay-only frames don't decrypt. Dispatch branches flood vs reactive on the toggle. gosim parity (bridge_flood_relay + flood_transport scenario field). Auth-gate test (bad hmac never relayed). 93/93 host, rpc 52/52, cppcheck/clang/board/gosim green. Incidental: fixed a gosim cross-test flake (sim_emitter quiet flag) at root in bridge_init.
CRITICAL CONCERN TO RESOLVE (blocks Task 4 validation): firmware channel_flood_decide has NO suppress=2 heard-count mechanism -> only the Go MODEL does. The winning numbers (100/100@25) DEPEND on suppress=2. Firmware flood will NOT match the model until firmware gets the heard-count suppression. Must scope + build this (fold into next task). Also: test/CMakeLists CONFIG_BRAMBLE_RPC_MAX_METHODS=40 stale (52 registered) -> harmless follow-up.
=> Dispatched independent Opus agent to confirm Task 1 + scope the firmware suppression gap.
Task 1 ACCEPTED (independent Opus review: all a-e confirmed against source, tests non-vacuous, 93/93 host + gosim green). 
NEXT TASK (Task 1.5, prerequisite for Task 4 validation): add heard-count rebroadcast suppression to match the model (cancel own pending relay after overhearing >=2 copies). SCOPE (from review, file-anchored):
- FIRMWARE: channel_flood_decide is stateless (only is_duplicate). Add flood_key + heard to pending_flood_relay_t (mesh_task.c:311-317); schedule_flood_relay records flood_key (packet_id^src_addr, caller has it at :2061) + heard=0. Increment the count in the s_dedup duplicate-hit branch (mesh_task.c:3105-3151, already reads src_addr for re-ACK at :3134): compute flood_key, on match to a queued s_flood_relay_queue entry do heard++; if >=2 cancel (used=false). TRICKY: overheard copies are consumed by s_dedup BEFORE handle_data, so must count THERE not in the flood block. Threshold=2 named constant mirroring floodSuppressAfterHeard.
- GOSIM bridge.c parity (the real catch, else Task 4 fails): bridge_flood_relay (:697-745) has no suppression; bridge dispatch dedup is RREQ-only so dup DATA DOES reach _handle_data -> increment heard there, but cancelling a queued EVT_SEND_PACKET needs a per-node pending map + canceled flag (mirror flood.go floodSim.pending / handleFloodRelayDue). Validate bridge suppression reproduces the model's relaysCanceled.
Also carry: test/CMakeLists CONFIG_BRAMBLE_RPC_MAX_METHODS=40 stale (52 registered) -> harmless follow-up.
