# Flooding F1 (unicast flood transport) progress ledger
Design: ~/src/internal-planning/plans/2026-07-04-flooding-substrate-design.md
Plan: ~/src/internal-planning/plans/2026-07-04-flooding-F1-plan.md
Base main: 7cc27ca0 (post PR #125 tooling). Branch: feat/flooding-f1.
PIVOT: flooding substrate (owner-committed). Build unicast flood + flooded-ACK behind s_flood_transport toggle (default OFF, coexist w/ reactive). Prove firmware flood through gosim matches the MODEL (n=25 100/100, n=50 80/80, n=100 90/90). Reactive removal is F3.
Reuse Phase 1 channel_flood_decide + s_flood_dedup (suppress=2). Authenticated flood (data_auth_verify before relay). NO parallel impl. 
Verify per task: host suite + board build + cppcheck(runner) + clang-format(runner v14) + check-rpc-contract + gosim go test -count=1.

Task 1 (unicast DATA floods behind toggle): DONE. See task-1-report.md. s_flood_transport (NVS bramble_flood, RPC bramble.setFloodTransport, reflected in getConfig floodTransportEnabled). handle_data's broadcast flood-relay block generalized to also cover unicast-for-other under the toggle (dest_is_broadcast || (s_flood_transport && !dest_is_self)); same channel_flood_decide + s_flood_dedup, no second impl. mesh_process_rx_packet's DATA_RX_FORWARD branch picks handle_data (flood) vs forward_data_packet (reactive) by the toggle. gosim bridge.c mirrors via g_flood_transport_enabled + shared bridge_flood_relay() helper + scenario field "flood_transport". Fixed a pre-existing cross-test flake (sim_emitter g_emitter_quiet leaking from radioHarness tests into later Sim() tests within the same process) at the root in bridge_init(). All green: 93/93 host, rpc 52/52, cppcheck clean, board build clean, gosim go test -count=1 green x3. Concern flagged: firmware channel_flood_decide has no suppress=2 heard-count mechanism (only gosim's Go model does); pre-existing Phase 1 gap, not introduced/fixed here.
Task 1: DONE (commit 71aabcde, Sonnet). s_flood_transport toggle (NVS + bramble.setFloodTransport RPC + getConfig floodTransportEnabled + openapi). Unicast-for-others reuses channel_flood_decide via the generalized broadcast guard (dest_broadcast || (flood && !self)) + early-return so relay-only frames don't decrypt. Dispatch branches flood vs reactive on the toggle. gosim parity (bridge_flood_relay + flood_transport scenario field). Auth-gate test (bad hmac never relayed). 93/93 host, rpc 52/52, cppcheck/clang/board/gosim green. Incidental: fixed a gosim cross-test flake (sim_emitter quiet flag) at root in bridge_init.
CRITICAL CONCERN TO RESOLVE (blocks Task 4 validation): firmware channel_flood_decide has NO suppress=2 heard-count mechanism -> only the Go MODEL does. The winning numbers (100/100@25) DEPEND on suppress=2. Firmware flood will NOT match the model until firmware gets the heard-count suppression. Must scope + build this (fold into next task). Also: test/CMakeLists CONFIG_BRAMBLE_RPC_MAX_METHODS=40 stale (52 registered) -> harmless follow-up.
=> Dispatched independent Opus agent to confirm Task 1 + scope the firmware suppression gap.
Task 1 ACCEPTED (independent Opus review: all a-e confirmed against source, tests non-vacuous, 93/93 host + gosim green). 
NEXT TASK (Task 1.5, prerequisite for Task 4 validation): add heard-count rebroadcast suppression to match the model (cancel own pending relay after overhearing >=2 copies). SCOPE (from review, file-anchored):
- FIRMWARE: channel_flood_decide is stateless (only is_duplicate). Add flood_key + heard to pending_flood_relay_t (mesh_task.c:311-317); schedule_flood_relay records flood_key (packet_id^src_addr, caller has it at :2061) + heard=0. Increment the count in the s_dedup duplicate-hit branch (mesh_task.c:3105-3151, already reads src_addr for re-ACK at :3134): compute flood_key, on match to a queued s_flood_relay_queue entry do heard++; if >=2 cancel (used=false). TRICKY: overheard copies are consumed by s_dedup BEFORE handle_data, so must count THERE not in the flood block. Threshold=2 named constant mirroring floodSuppressAfterHeard.
- GOSIM bridge.c parity (the real catch, else Task 4 fails): bridge_flood_relay (:697-745) has no suppression; bridge dispatch dedup is RREQ-only so dup DATA DOES reach _handle_data -> increment heard there, but cancelling a queued EVT_SEND_PACKET needs a per-node pending map + canceled flag (mirror flood.go floodSim.pending / handleFloodRelayDue). Validate bridge suppression reproduces the model's relaysCanceled.
Also carry: test/CMakeLists CONFIG_BRAMBLE_RPC_MAX_METHODS=40 stale (52 registered) -> harmless follow-up.
Task 1.5 (suppression) DONE (commit 6a4dd4cb, Opus). FLOOD_SUPPRESS_AFTER=2 heard-count cancellation: firmware (pending_flood_relay_t +flood_key/heard; count in the s_dedup dup-hit branch; cancel=used=false; pure helper channel_flood_note_overheard) + gosim-bridge parity (per-node flood_pending[] map, flood_key on the relay event's dest_addr field, cancel at due-time via flood_relay_suppressed). src-qualified key; first/own copies not miscounted. 5 host + 2 gosim tests. 93/93 + all CI green.
CONSTRAINTS TO CARRY (from review, affect validation not correctness):
- SUPPRESSION ONLY FIRES when jitter window > frame airtime. SF10/125k: ~490ms airtime > 300ms jitter -> NEVER fires (flooding behaves as suppress=none = every node relays). SF7/fast: ~30ms airtime < jitter -> fires. Winning numbers were SF7 dense (consistent). => the whole flood-suppression benefit is PHY-dependent; measure ONLY at fast-PHY dense (SF7). Reinforces: SF-matched-to-density is the master variable.
- firmware jitter (flat 50-300ms) != model jitter (SNR-derived 100-549ms) -> won't match model copy-for-copy. REFRAME Task 4: firmware is now source of truth; validate = firmware flood delivers ~80-100% confirmed at SF7 dense DIRECTLY (model is a reference, not an exact target). Jitter is itself a tuning lever (later).
- gosim reuses relay-event dest_addr to carry flood_key (safe today; note if a future EVT_SEND_PACKET path reads dest_addr).
- carry: test/CMakeLists CONFIG_BRAMBLE_RPC_MAX_METHODS=40 stale (52 registered).
=> NEXT: Task 2 flooded-ACK (confirmation) to Opus. Then Task 3 send-side, Task 4 validation (SF7 dense, firmware direct), Task 5 docs+competitive delta.
Task 2 (flooded-ACK) DONE (commit 1cce03af, Opus). handle_ack not-for-us branch under flood floods the ACK via channel_flood_decide + schedule_flood_relay (shared queue gained tx_kind for DATA=BROADCAST / ACK=CRITICAL lanes) + suppression; new PKT_TYPE_ACK dispatch-gate branch feeds channel_flood_note_overheard. ack_verify runs BEFORE relay (bad-MAC ACK never rebroadcast; non-vacuous 1-byte-flip test). Sender correlates via existing pending_ack_remove(ack_packet_id) (route-free). Re-ACK second chance preserved (fresh packet_id). gosim proof TestFloodedAckConfirmsSenderWithoutRoutes: receipt floods D->C->B->A, no route lookups, sender confirmed. 94/94 host + gosim green. Concerns: empty relay_path cosmetic; send-side still reactive (Task 3); suppression needs fast PHY (Task 4 validate at SF7-dense).
DECISION: independent verification concentrated in the F1 WHOLE-BRANCH review before PR (both auth-before-relay gates + suppression + toggle isolation), not per-task, since Task 2 mirrors verified Task 1.
=> Task 3 (send-side flood origination + confirmation/retry lifecycle) to Opus.
Task 3 (send-side flood origination + confirmation/retry lifecycle) DONE (Opus). mesh_send_message gates the reactive neighbor/route + RREQ/queue block on !s_flood_transport; under flood it falls straight through to mesh_send_channel -> send_data_packet/send_dm_packet (DATA dest=D, hop_limit=ROUTE_HOP_LIMIT_MAX, auth-signed, AEAD, single mesh_tx broadcast). No retry-loop change needed: pending_ack_tick re-transmits the stored broadcast frame = a re-flood (mesh_tx never route-looks-up; same packet_id -> dest re-ACK-on-duplicate). KE unchanged and just works: send_ke_envelope->send_data_packet already floods; skipping the route gate lets mesh_send_dm initiate the handshake under empty routes; INIT/RESP/session-flush all flood; KE stays CRITICAL tier. Exactly-once = Phase 1 s_delivered_dedup re-ACK (re-flood re-ACKs, never re-delivers), untouched. gosim bridge.c parity: generate_message + retransmit flood-branch (broadcast, bypass forward_data) when g_flood_transport_enabled. Tests: 6 host (test_flood_origination.c: no-discovery+pending, reactive A/B, no-ACK->3 refloods->FAILED, ACK->DELIVERED, dropped-first-ACK->reflood->DELIVERED, KE critical 8 refloods) + 2 gosim (flood_origination_test.go: A floods origination broadcast + confirmed with EMPTY routes; off A/B routes-first). 95/95 host + board + cppcheck + clang-format v14 + rpc-contract + gosim all green. Concerns: reflood vs 60s DEDUP_EXPIRY_MS means retry mainly helps the loss case (F2 tuning); gosim retransmit attempt==0 guard means its pending-ack retry rarely fires (confirms on first flood+receipt anyway).
=> F1 Tasks 1-3 complete (unicast flood transport + flooded-ACK + flood origination, all behind s_flood_transport). NEXT: Task 4 validation (firmware flood through gosim vs model, SF7 dense 25/50/100), Task 5 docs+competitive delta. Then WHOLE-BRANCH review before PR.
