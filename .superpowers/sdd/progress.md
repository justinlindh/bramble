# Mandatory attestation (bootstrap-quorum-race close) ledger
Plan: ~/src/internal-planning/plans/2026-07-05-mandatory-attestation-plan.md. Base main 2969df02. Branch feat/mandatory-attestation.
Owner: bounded bootstrap-race close (NOT the trust anchor). Honest: closes the unattested-trust path + bounds the unpinned-trust window to a per-boot grace; does NOT stop Sybil minting (anchor pending).
Core: identity_store_quorum_eligible zero-pins "trust all" (unbounded) -> bounded boot grace, then pinned-only. Liveness: fresh mesh must still bootstrap timesync; transport/relay/first-contact stay OPEN.
CI GOTCHA: push diff must touch path-matched file, not only .superpowers scratch.
Tasks: 1 bootstrap-grace in quorum + tests | 2 trust-surface inventory (mailbox) + docs + delta.
Task 1 (bootstrap-quorum grace): COMPLETE (commit ecf34b05). INLINE review: sound. quorum_eligible(s,addr,established,now_ms) = established && (pinned || within-boot-grace); after grace unpinned NEVER eligible (race closed); wraparound-safe. boot_ms via identity_store_init(s,now_ms()) at boot (mesh:5602); caller :1486 passes t. QUORUM_BOOTSTRAP_GRACE_MS=300000 (5min). Security test asserts FALSE at exactly boot+GRACE w/ zero pins (non-vacuous); liveness test asserts TRUE within grace. Early-exit refinement deferred (clean 1-liner). HONEST LIMIT: gosim has no timesync model -> liveness is host-pure-function-tested only (noted). 101 host + board + gosim green.
Task 2 base = 70bc93b8. T2 = inventory other Sybil/unattested trust surfaces (mailbox custody primary); gate-or-justify (keep transport/relay/first-contact OPEN); SECURITY-MODEL NEW-SEC-4 update (bootstrap race CLOSED->bounded grace; honest: Sybil minting still free w/o anchor, per-boot grace = residual exposure); competitive delta.
Task 2 (trust-surface inventory + honest docs + delta): COMPLETE (docs-only, 1 commit). DECISION: mailbox custody LEFT OPEN (justified, not gated). Reasoning: opt-in (s_mailbox_enabled default false); outsider-proof already (data_auth_verify gates before mesh_mailbox_store, only network-key DATA stored); self-bounded (32-slot static array, 8/source, 8/dest, 24h TTL, LRU, no dyn alloc); custody grants no trust (deferred verbatim DATA, dest re-auths). Gating would harm liveness (partition/bootstrap S&F) AND buy nothing (Sybil mints pinnable IDs free). No code change -> no mailbox test needed. mesh_task.c:1412 comment already updated by Task1 (ecf34b05), verified. NEW-SEC-4 in SECURITY-MODEL.md reframed: bootstrap RACE closed by bounded per-boot grace; 3 residuals stated (1 minting still free, 2 per-boot grace = bounded exposure, 3 route/RREP trust out of scope) + uniform-attestation principle. Cross-refs (headings sec3/sec4, mailbox section, COMPARISON delta) updated. ASCII-only, no em dash. Neighbor-table admission noted as transport-plane (not a trust decision), out of scope.
