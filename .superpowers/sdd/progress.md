# Ed25519 per-node identity (campaign) progress ledger
Plan: ~/src/internal-planning/plans/2026-07-05-ed25519-identity-plan.md. Base main d699c273 (post 398bbdf3). Branch feat/ed25519-identity.
Decisions: D0 airtime=YES; D2 rebind address to Ed25519 (flag day); D3 ship phases 0-4, defer Sybil phase 5.
Primitive: device=espressif/libsodium, host=OpenSSL EVP_PKEY_ED25519. Keygen via entropy-gated crypto_random seed. Shared RFC8032 KAT+malleability vectors pin host==device.
CI: gitea-runner wedges -> docker restart + fresh push (runbooks/gitea-api.md).
Phases: 0 primitive | 1 keypair+NVS+address-rebind | 2 attestation frame | 3 verify+TOFU-pin | 4 gate timesync/DM on identity.
--
Phase 0 (primitive) DONE. See task-p0-report.md. crypto_ed25519_{keypair,sign,verify} + BRAMBLE_ED25519_{PUBKEY(32),SECKEY(64),SIG(64)}_SIZE in crypto.h; device=libsodium crypto_sign_* (new components/crypto/idf_component.yml espressif/libsodium ^1.0.20 -> 1.0.22~1, REQUIRES espressif__libsodium), host=OpenSSL EVP_PKEY_ED25519. sk format = libsodium seed||pk (host consumes seed half; tested invariant). Keygen seed via crypto_random -> crypto_entropy_fill(esp_random) gate, fail-closed scratch-buffer pattern mirroring crypto_generate_identity. test/test_ed25519.c: RFC8032 7.1 TEST 1/2/3/SHA(abc) exact-byte KAT + S+L malleation rejected + flipped-byte + roundtrip/wrong-key/tampered-msg (TDD red->green). 97/97 host, board build linked libsodium (symbol-level verified; crypto_ed25519_* GC'd until Phase 1 calls them), cppcheck+clang-format-v14(runner)+rpc-contract+gosim green. CARRY: small-order-point verdict divergence (libsodium rejects, OpenSSL accepts) documented; constrain Phase 3 verify semantics accordingly.
Task 0 (Ed25519 primitive): COMPLETE (commit 633137ef, base 398bbdf3). Independent review (Opus): spec MET, quality APPROVED, no Critical/Important. RFC8032 vectors verified genuine; entropy gate fail-closed confirmed; malleability non-vacuous. MINOR (for final review): test executes host backend only (no on-device runner; device pinned by construction); hex_to_bytes ignores sscanf ret (harmless). CARRY-FORWARD: libsodium rejects small-order pubkeys/R, OpenSSL doesn't -> constrains Phase 3 verify semantics.
Task 1 base = 633137ef.
PHASE REORDER: rebind moved P1->P3 (peer-address resolution needs the attestation's Ed keys). P1 = keypair+NVS+gen+pubkey_hash-fix, address unchanged.
Task 1 (Ed keypair+NVS+pubkey_hash fix): COMPLETE (commits 09612dec..5e0c1cb3). INLINE controller review: spec MET, approved. Pinned hash constants verified genuine (controller recomputed SHA256(00..1f)). Fail-closed both backends; migration self-heals on partial write; host store refactor = real save/load now unit-tested (was stubs). MINOR (final review): partial X25519 key bytes remain in caller struct on generate failure (pre-existing).
Task 2 base = 5e0c1cb3.
Task 2 (attestation frame + origination): COMPLETE (commits 673e6322..e317ea34). INLINE controller review: approved. 144B frame exact-length; shared signed-msg builder (signer==verifier bytes); context string verified 16B; tamper tests non-vacuous (control-verify-then-mutate); budget-gated origination w/ 60s retry + regen re-announce. FINDING: attestations are NEIGHBORS-ONLY this phase (RX dispatch relays per-type; unknown types drop) -> P3 must extend relay.
P3 DESIGN INPUTS (controller):
 (a) RELAY GATE: extending relay raises a tension with the branch invariant "keyless traffic never propagates" (the attestation deliberately has no network-key MAC). Resolution: ADD an outer network-key MAC + seq to the frame IN P3 (frame is new/additive, no compat cost yet): relays verify the CHEAP MAC before flooding (membership gates relay privilege); receivers verify MAC + Ed sig before pinning (sig carries the claim's truth). Preserves the invariant; outsider spam cannot be amplified.
 (b) REPLAY: attestation is replayable by design (no freshness in signed bytes); pinning must be IDEMPOTENT (same content = no-op), conflict only on a DIFFERENT key for a pinned address. The outer seq + control replay window bounds relay-layer replay.
Task 3 base = e317ea34.
Task 3 (relay gate + flood relay + TOFU pin + conflict): COMPLETE (commits 2bd007d8..7ddd6c48). INLINE controller review: approved. Handler order verified (deserialize->MAC->replay->dedup->deliver->relay); relays never Ed-verify; store self-check + single Ed-verify via shared canonical builder; conflict test non-vacuous (control-pin first, forged refused, original survives). Residuals recorded: RAM-only pins, MAC-valid-sig-invalid relays (budget-bounded), heard-impersonator-first TOFU window, counters C-API-only.
Task 4 base = 7ddd6c48. P4 = ADDRESS REBIND to Ed25519 + gate timesync/neighbor-established/DM-peer on pinned identity + docs + competitive delta. Rebind nuance: with address=derive(ed_pub), receivers ALSO check att.src_addr==derive(att.ed25519_pub), making impersonation-of-an-address CRYPTOGRAPHICALLY impossible for insiders (beats TOFU-first-seen). Implementer must inventory crypto_derive_address callers (X25519-coupling, esp. KEY_EXCHANGE/DM peer matching + gosim node identity) and reconcile each.
