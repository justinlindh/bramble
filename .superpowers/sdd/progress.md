# Mandatory provisioning (drop public-PSK fallback) ledger
Plan: ~/src/internal-planning/plans/2026-07-05-mandatory-provisioning-plan.md. Base main a01a2f68. Branch feat/mandatory-provisioning.
Decision: unprovisioned=INERT (fail-closed, no PSK fallback); first key = node-generates+displays OR operator-pastes; persist NVS. FLAG DAY accepted (pre-alpha).
KEY RISK: sim + host tests lean on the fallback key -> harness must provision a shared test key by default or everything goes inert.
CI GOTCHA: push DIFF must touch a path-matched file (main/components/docs/test/api/simulator/scripts), not only .superpowers scratch (else no run; do NOT restart infra).
Tasks: 1 network_key fail-closed+generate+NVS | 2 gate control plane inert + boot + harness | 3 RPC+webapp UX | 4 docs+delta.
Task 1 (network_key fail-closed core): COMPLETE (commits c4f142d0, 0480637a). INLINE review: sound. network_key_get unprovisioned->-1 no write; network_key_mac void->int, unprovisioned->all-zero sentinel + return -1 (callers must check return, esp. verify-side BEFORE ct_eq or all-zero matches); generate_provision entropy-gated crypto_random fail-closed; NVS load/persist added. PSK removed from network_key (grep clean). BRAMBLE_PUBLIC_CHANNEL_PSK KEPT (crypto.h) for the SEPARATE opt-in public-broadcast-channel feature (public_channel.c) - verified not a control-plane backdoor; but mesh_task.c:5450 beacon-key fallback DERIVES from it = a control-plane fallback Task 2 must make inert.
Task 2 base = 9d0ba53c. T2 MUST: (a) harden 12 mac call sites (routing_auth 45/52/77/84/109/116/135/142/165/172, discovery 141/148) - check int return; verify-side reject BEFORE compare; (b) kill beacon-key public-PSK fallback (mesh_task mesh_rederive_beacon_key ~5450) -> beacon inert unprovisioned; (c) gate ALL control send on provisioned (inert node); (d) boot: consolidate mesh_load_network_key + rpc_set_network_key onto network_key_load_from_nvs/persist; (e) HARNESS: 7 host suites (test_ack_auth/data_auth/discovery/ident_relay_auth/rerr_auth/rrep_auth/unicast_flood) provision a test key in setUp not clear(); gosim node init provisions scenario key default-on + an unprovisioned-inert scenario.
Task 2 (inert node + harness): COMPLETE (commits 9276ed8b..b158e643). INLINE review: sound. All 5 verify sites guard network_key_mac()!=0 -> return 0 BEFORE ct_eq (forgery-prevention correct). Beacon: unprovisioned skips + mesh_rederive_beacon_key zeroes key (PSK fallback GONE). 8 send-gates on is_provisioned. Boot delegates to network_key_load_from_nvs; rpc_set_network_key uses component persist. Harness: test/test_net_key.h fixed key in 11 auth suites' setUp; gosim bridge_init provisions fleet default + "unprovisioned":true scenario field + TestUnprovisionedNodeIsInert. 101/101 host, board, gosim green. Provisioned byte-identical (short-circuit gates). => RESIDUAL CLOSED IN FIRMWARE.
Task 3 base = b158e643. T3 = RPC generateNetworkKey (mint+persist+return key over LOCAL web channel + fingerprint) + setNetworkKey (exists, paste) + getStatus surfaces provisioned/fingerprint + openapi additive; webapp surfaces UNPROVISIONED prominently + generate/paste affordance + fingerprint for fleet-convergence.
Task 3 (generate RPC + webapp UX): COMPLETE (commits 87b917d1, 32849379). INLINE review: sound. generateNetworkKey fail-closed (entropy->RPC_ERR_INTERNAL), wipes key copy, never logs, re-derives beacon live; RPC path NOT provisioned-gated (provisioning works while inert). UnprovisionedBanner (role=alert, below topbar, all tabs, connected+!provisioned). Re-key warning requires explicit confirm. Fingerprint surfaced. openapi additive (54 methods). 101 host + board + webapp 273 tests + all CI green. MINOR (T4 cleanup): dead client-side helpers generateNetworkKeyHex/networkKeyFingerprint remain in utils/networkKeyShare (now unused; server-side generation replaced them).
Task 4 base = 3bdf9948. T4 = docs (SECURITY-MODEL: "forgeable-until-provisioned" residual CLOSED BY CONSTRUCTION, network_key no longer "staged/fallback"; spec; README; competitive delta = no insecure bootstrap / no public default key, greenfield property Meshtastic's shared-public-default-channel can't match) + REMOVE the dead client key-gen helpers + their tests.
Task 4 (docs + delta + cleanup): COMPLETE (commits 5a34021e cleanup, 2be5d18c docs, + this report). All claims verified vs code (network_key.c fail-closed get/mac + all-zero sentinel; routing_auth.c 5 verifies reject BEFORE ct_eq; mesh_task.c 8 send-gates + beacon zeroed key; rpc_methods.c on-device generateNetworkKey; PSK only in public_channel.c now). SECURITY-MODEL: keyless-outsider residual CLOSED BY CONSTRUCTION; insider forgery UNCHANGED (narrowed by prior Ed25519, not this work); public-broadcast-channel boundary stated. Spec 4.25 items 8/9 + wire-v2 beacon note fixed + Public Channel boundary callout. README + COMPARISON: 4 stale "falls back to public PSK" fixed + new strengths point 8 (greenfield no-insecure-bootstrap, honest boundaries). Cleanup: generateNetworkKeyHex + networkKeyFingerprint + hexToBytes + their test removed; share/QR codec kept. One stale mesh_task.c comment fixed (comment-only). CI: 101/101 host, board build (heltec-v3), clang-format v14 (379 files) + cppcheck, check-rpc-contract, gosim, webapp (typecheck + 272 unit + build) ALL GREEN. NO em dashes, ASCII only. => CAMPAIGN COMPLETE.
Task 4 (docs+delta+cleanup): COMPLETE (commits 5a34021e, 2be5d18c). INLINE review: docs honestly bounded (627 header scopes outsider-closed/insider-remains; 671 "does NOT close" insider; public-channel boundary stated; delta corrected 4 stale PSK claims + added "no insecure bootstrap" strength). Dead client key-gen helpers removed. T4 firmware = 1 comment only. All CI green.
=> ALL 4 TASKS DONE. Whole-branch review next, then merge.
