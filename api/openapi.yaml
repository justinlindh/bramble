openapi: 3.1.0
info:
  title: Bramble Mesh Node API
  description: >
    JSON-RPC 2.0 API for Bramble mesh nodes.
    Methods are invoked via JSON-RPC over serial (UART), WebSocket, or BLE.
    Each method is mapped to a POST path for codegen and documentation purposes.
    The actual wire format is standard JSON-RPC 2.0.
  version: 0.1.0
  license:
    name: MIT

paths:
  # ── Query Methods ──────────────────────────────────────────────────────────

  /rpc/bramble.getStatus:
    post:
      operationId: getStatus
      summary: Get node status
      description: >
        Returns current node status including uptime, packet counters,
        neighbor/route counts, airtime usage, and GPS position.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Node status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponse'

  /rpc/bramble.getIdentity:
    post:
      operationId: getIdentity
      summary: Get node identity
      description: Returns the node's address and public key hash.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Node identity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResponse'

  /rpc/bramble.getVersion:
    post:
      operationId: getVersion
      summary: Get firmware version info
      description: Returns firmware version, protocol version, and hardware identifier.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Version info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VersionResponse'

  /rpc/bramble.getNeighbors:
    post:
      operationId: getNeighbors
      summary: Get direct radio neighbors
      description: Returns list of nodes heard directly via radio with signal quality metrics.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Neighbor list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NeighborsResponse'

  /rpc/bramble.getRoutes:
    post:
      operationId: getRoutes
      summary: Get routing table
      description: Returns all known routes including direct and multi-hop paths.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Route table
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoutesResponse'

  /rpc/bramble.getMessages:
    post:
      operationId: getMessages
      summary: Get stored messages
      description: Returns messages in the node's message store.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Message list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessagesResponse'

  /rpc/bramble.getAirtime:
    post:
      operationId: getAirtime
      summary: Get airtime budget status
      description: Returns per-tier airtime budget usage and remaining allocation.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Airtime stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AirtimeResponse'

  /rpc/bramble.getPeerLocations:
    post:
      operationId: getPeerLocations
      summary: Get peer location data
      description: Returns location information for all known peers, varying by sharing tier.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Peer locations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeerLocationsResponse'

  /rpc/bramble.ping:
    post:
      operationId: ping
      summary: Ping the node
      description: Health check. Returns pong with node address and protocol version.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Pong response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PingResponse'

  # ── Config / Action Methods ────────────────────────────────────────────────

  /rpc/bramble.getConfig:
    post:
      operationId: getConfig
      summary: Get full node configuration
      description: Returns identity, radio settings, channels, mailbox, and location config.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Full configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigResponse'

  /rpc/bramble.sendMessage:
    post:
      operationId: sendMessage
      summary: Send a direct text message
      description: >
        Sends a unicast message to a destination address.
        Returns immediately with a packet identifier; delivery status arrives
        via bramble.onAck notification.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMessageParams'
      responses:
        '200':
          description: Message queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendMessageResponse'

  /rpc/bramble.sendBroadcast:
    post:
      operationId: sendBroadcast
      summary: Broadcast a text message
      description: Sends a broadcast message on the public channel.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendBroadcastParams'
      responses:
        '200':
          description: Broadcast queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendBroadcastResponse'

  /rpc/bramble.sendProbe:
    post:
      operationId: sendProbe
      summary: Send a network probe
      description: >
        Broadcasts a probe packet. Responses arrive asynchronously as
        probe.ack notifications, followed by a probe.complete notification.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Probe initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendProbeResponse'

  /rpc/bramble.setRadio:
    post:
      operationId: setRadio
      summary: Update radio parameters
      description: Sets one or more radio parameters (frequency, spreading factor, bandwidth, TX power).
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetRadioParams'
      responses:
        '200':
          description: Radio updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setNodeName:
    post:
      operationId: setNodeName
      summary: Set the node display name
      description: Sets the node name (max 8 characters).
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetNodeNameParams'
      responses:
        '200':
          description: Name updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.addChannel:
    post:
      operationId: addChannel
      summary: Add a channel
      description: Adds a new channel with the given name and pre-shared key.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddChannelParams'
      responses:
        '200':
          description: Channel added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddChannelResponse'

  /rpc/bramble.removeChannel:
    post:
      operationId: removeChannel
      summary: Remove a channel
      description: Removes a channel by index.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoveChannelParams'
      responses:
        '200':
          description: Channel removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setDefaultChannel:
    post:
      operationId: setDefaultChannel
      summary: Set the default channel
      description: Sets which channel is used by default for outgoing messages.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetDefaultChannelParams'
      responses:
        '200':
          description: Default channel updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setMailbox:
    post:
      operationId: setMailbox
      summary: Enable or disable mailbox mode
      description: Toggles store-and-forward mailbox functionality.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetMailboxParams'
      responses:
        '200':
          description: Mailbox setting updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setLocationConfig:
    post:
      operationId: setLocationConfig
      summary: Configure location sharing
      description: Enables/disables location sharing and sets default parameters.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetLocationConfigParams'
      responses:
        '200':
          description: Location config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setLocationContact:
    post:
      operationId: setLocationContact
      summary: Add or update a location contact
      description: Sets the sharing tier and parameters for a specific peer address.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetLocationContactParams'
      responses:
        '200':
          description: Contact updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.removeLocationContact:
    post:
      operationId: removeLocationContact
      summary: Remove a location contact
      description: Stops sharing location with the specified peer.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoveLocationContactParams'
      responses:
        '200':
          description: Contact removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.shareLocationOnce:
    post:
      operationId: shareLocationOnce
      summary: Share location once with a peer
      description: Sends a one-time location update to the specified address.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShareLocationOnceParams'
      responses:
        '200':
          description: Location shared
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.reboot:
    post:
      operationId: reboot
      summary: Reboot the node
      description: Triggers a software reboot of the node.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '200':
          description: Reboot initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

components:
  schemas:
    # ── Primitives / Empty ───────────────────────────────────────────────────

    EmptyParams:
      type: object
      description: No parameters required.
      properties: {}

    OkResponse:
      type: object
      description: Generic success response.
      required: [ok]
      properties:
        ok:
          type: boolean
          description: Always true on success.

    # ── Reusable Types ───────────────────────────────────────────────────────

    Position:
      type: object
      description: GPS position with accuracy and motion data.
      required: [lat, lon, alt, accuracy, timestampMs]
      properties:
        lat:
          type: number
          description: Latitude in decimal degrees.
        lon:
          type: number
          description: Longitude in decimal degrees.
        alt:
          type: number
          description: Altitude in meters.
        accuracy:
          type: number
          description: Horizontal accuracy in meters.
        speed:
          type: number
          description: Speed in meters per second.
        heading:
          type: number
          description: Heading in degrees (0-360).
        timestampMs:
          type: integer
          description: Position fix timestamp (Unix ms).

    Neighbor:
      type: object
      description: A directly heard radio neighbor.
      required: [addr, rssi, snr, lastHeardMs]
      properties:
        addr:
          type: integer
          description: Neighbor address (uint32).
        rssi:
          type: integer
          description: Received signal strength (dBm).
        snr:
          type: number
          description: Signal-to-noise ratio (dB).
        deliveryRate:
          type: integer
          description: Link delivery rate metric (0-255).
        lastHeardMs:
          type: integer
          description: Milliseconds since last packet received.
        isMailbox:
          type: boolean
          description: Whether this neighbor acts as a mailbox.
        airtimeRemaining:
          type: integer
          description: Estimated airtime remaining (percentage).

    Route:
      type: object
      description: A routing table entry.
      required: [dest, nextHop, hopCount, metric, state, lastUsedMs]
      properties:
        dest:
          type: integer
          description: Destination address (uint32).
        nextHop:
          type: integer
          description: Next-hop address (uint32).
        hopCount:
          type: integer
          description: Number of hops to destination.
        metric:
          type: integer
          description: Route cost metric (lower is better).
        state:
          type: string
          enum: [active, stale, discovering]
          description: Route state.
        lastUsedMs:
          type: integer
          description: Milliseconds since route was last used.

    Channel:
      type: object
      description: A configured channel.
      required: [index, name, hasPsk, epoch, isDefault]
      properties:
        index:
          type: integer
          description: Channel index.
        name:
          type: string
          description: Channel display name.
        hasPsk:
          type: boolean
          description: Whether a pre-shared key is configured.
        epoch:
          type: integer
          description: Key epoch counter.
        isDefault:
          type: boolean
          description: Whether this is the default channel.

    Message:
      type: object
      description: A stored or incoming message.
      required: [from, to, text, timestamp]
      properties:
        from:
          type: integer
          description: Sender address (uint32).
        to:
          type: integer
          description: Destination address (uint32, 0xFFFFFFFF for broadcast).
        text:
          type: string
          description: Message text content.
        tier:
          type: string
          enum: [critical, normal]
          description: Message priority tier.
        timestamp:
          type: integer
          description: Message timestamp (Unix seconds).
        msgId:
          type: string
          description: Unique message identifier.

    AirtimeTier:
      type: object
      description: Airtime budget for a single priority tier.
      required: [name, remainingMs, maxMs, usedPct, refillAtMs]
      properties:
        name:
          type: string
          enum: [critical, normal, broadcast]
          description: Tier name.
        remainingMs:
          type: integer
          description: Remaining airtime budget in milliseconds.
        maxMs:
          type: integer
          description: Maximum airtime budget in milliseconds.
        usedPct:
          type: integer
          description: Percentage of budget used.
        refillAtMs:
          type: integer
          description: Unix timestamp (ms) when budget refills.

    LocationContact:
      type: object
      description: A location sharing contact configuration.
      required: [addr, tier, intervalSec, distanceTriggerM]
      properties:
        addr:
          type: integer
          description: Peer address (uint32).
        tier:
          type: string
          enum: [full, coarse, presence]
          description: Location sharing tier.
        intervalSec:
          type: integer
          description: Update interval in seconds.
        distanceTriggerM:
          type: integer
          description: Minimum distance change (meters) to trigger update.

    LocationPeer:
      type: object
      description: Location data for a peer node.
      required: [addr, name, tier, online, lastUpdatedMs]
      properties:
        addr:
          type: integer
          description: Peer address (uint32).
        name:
          type: string
          description: Peer display name.
        tier:
          type: string
          enum: [full, coarse, presence]
          description: Location sharing tier.
        position:
          oneOf:
            - $ref: '#/components/schemas/Position'
            - type: 'null'
          description: Full position data (null if tier is coarse or presence).
        gridSquare:
          type: string
          description: Maidenhead grid square (for coarse tier).
        online:
          type: boolean
          description: Whether the peer is currently online.
        lastUpdatedMs:
          type: integer
          description: Unix timestamp (ms) of last update.

    RelayHop:
      type: object
      description: A hop in a relay path.
      required: [addr, rssi]
      properties:
        addr:
          type: integer
          description: Relay node address (uint32).
        rssi:
          type: integer
          description: RSSI at this hop (dBm).

    # ── Response Schemas ─────────────────────────────────────────────────────

    StatusResponse:
      type: object
      description: Node status information.
      required: [uptimeSec, freeHeapBytes, fwVersion, txCount, rxCount, droppedCount, neighborCount, routeCount, airtimeUsedMs]
      properties:
        uptimeSec:
          type: integer
          description: Seconds since boot.
        freeHeapBytes:
          type: integer
          description: Free heap memory in bytes.
        fwVersion:
          type: string
          description: Firmware version string.
        txCount:
          type: integer
          description: Total packets transmitted.
        rxCount:
          type: integer
          description: Total packets received.
        droppedCount:
          type: integer
          description: Total packets dropped.
        neighborCount:
          type: integer
          description: Number of direct neighbors.
        routeCount:
          type: integer
          description: Number of active routes.
        airtimeUsedMs:
          type: integer
          description: Total airtime used in milliseconds.
        position:
          $ref: '#/components/schemas/Position'

    IdentityResponse:
      type: object
      description: Node identity information.
      required: [address, pubkey_hash]
      properties:
        address:
          type: string
          description: Node address as hex string (e.g. "1A2B3C4D").
        pubkey_hash:
          type: string
          description: Public key hash as hex string.

    VersionResponse:
      type: object
      description: Firmware version information.
      required: [firmware_version, protocol_version, hardware]
      properties:
        firmware_version:
          type: string
          description: Firmware version string.
        protocol_version:
          type: string
          description: Protocol version string.
        hardware:
          type: string
          description: Hardware platform identifier.

    PingResponse:
      type: object
      description: Ping/pong response.
      required: [pong, address, protocol_version]
      properties:
        pong:
          type: boolean
          description: Always true.
        address:
          type: string
          description: Node address as hex string.
        protocol_version:
          type: string
          description: Protocol version string.

    NeighborsResponse:
      type: object
      required: [neighbors]
      properties:
        neighbors:
          type: array
          items:
            $ref: '#/components/schemas/Neighbor'

    RoutesResponse:
      type: object
      required: [routes]
      properties:
        routes:
          type: array
          items:
            $ref: '#/components/schemas/Route'

    MessagesResponse:
      type: object
      required: [messages]
      properties:
        messages:
          type: array
          items:
            $ref: '#/components/schemas/Message'

    AirtimeResponse:
      type: object
      description: Current airtime budget state (flat firmware format).
      required:
        - critical_remaining_ms
        - normal_remaining_ms
        - broadcast_remaining_ms
        - critical_max_ms
        - normal_max_ms
        - broadcast_max_ms
        - next_refill_ms
      properties:
        critical_remaining_ms:
          type: integer
        normal_remaining_ms:
          type: integer
        broadcast_remaining_ms:
          type: integer
        critical_max_ms:
          type: integer
        normal_max_ms:
          type: integer
        broadcast_max_ms:
          type: integer
        next_refill_ms:
          type: integer

    PeerLocationsResponse:
      type: object
      required: [peers]
      properties:
        peers:
          type: array
          items:
            $ref: '#/components/schemas/LocationPeer'

    ConfigResponse:
      type: object
      description: Full node configuration.
      required: [identity, radio, channels, mailboxEnabled, location]
      properties:
        identity:
          type: object
          required: [address, pubkeyHash, name, pubkeyB64]
          properties:
            address:
              type: integer
              description: Node address (uint32).
            pubkeyHash:
              type: integer
              description: Public key hash (uint32).
            name:
              type: string
              description: Node display name.
            pubkeyB64:
              type: string
              description: Base64-encoded public key.
        radio:
          type: object
          required: [txPowerDbm, sf, bwKhz, cr, freqMhz]
          properties:
            txPowerDbm:
              type: integer
              description: Transmit power in dBm.
            sf:
              type: integer
              description: LoRa spreading factor (7-12).
            bwKhz:
              type: integer
              description: Bandwidth in kHz (125, 250, or 500).
            cr:
              type: integer
              description: Coding rate (5-8).
            freqMhz:
              type: number
              description: Frequency in MHz.
        channels:
          type: array
          items:
            $ref: '#/components/schemas/Channel'
        mailboxEnabled:
          type: boolean
          description: Whether store-and-forward mailbox is enabled.
        location:
          type: object
          required: [enabled, contacts, defaultIntervalSec, defaultDistanceTriggerM, stationaryBackoff]
          properties:
            enabled:
              type: boolean
              description: Whether location sharing is enabled.
            contacts:
              type: array
              items:
                $ref: '#/components/schemas/LocationContact'
            defaultIntervalSec:
              type: integer
              description: Default update interval in seconds.
            defaultDistanceTriggerM:
              type: integer
              description: Default distance trigger in meters.
            stationaryBackoff:
              type: integer
              description: Backoff multiplier when stationary.

    # ── Request Params ───────────────────────────────────────────────────────

    SendMessageParams:
      type: object
      description: Parameters for sending a direct message.
      required: [dest, text]
      properties:
        dest:
          type: string
          description: Destination node address as 8-char uppercase hex (e.g. "04CAAAF8").
        text:
          type: string
          description: Message text content.

    SendMessageResponse:
      type: object
      required: [packetId, status]
      properties:
        packetId:
          type: string
          description: Packet ID as uppercase hex string.
        status:
          type: string
          enum: [sent]

    SendBroadcastParams:
      type: object
      required: [text]
      properties:
        text:
          type: string

    SendBroadcastResponse:
      type: object
      required: [message_id, status, broadcast, channel]
      properties:
        message_id:
          type: string
        status:
          type: string
          enum: [sent]
        broadcast:
          type: boolean
        channel:
          type: integer

    SendProbeResponse:
      type: object
      required: [probeId, ackWindow]
      properties:
        probeId:
          type: integer
          description: Probe identifier.
        ackWindow:
          type: integer
          description: Seconds to wait for probe responses.

    SetRadioParams:
      type: object
      description: Radio parameters to update. All fields optional.
      properties:
        tx_power_dbm:
          type: integer
          description: Transmit power in dBm.
        sf:
          type: integer
          description: LoRa spreading factor (6-12).
        bw_hz:
          type: integer
          description: Bandwidth in Hz (125000, 250000, 500000).
        coding_rate:
          type: integer
          description: Coding rate denominator (5-8).
        frequency_mhz:
          type: number
          description: Frequency in MHz.

    SetNodeNameParams:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Node display name (max 8 characters).
          maxLength: 8

    AddChannelParams:
      type: object
      required: [name, psk]
      properties:
        name:
          type: string
          description: Channel display name.
        psk:
          type: string
          description: Pre-shared key (base64 or hex encoded).

    AddChannelResponse:
      type: object
      required: [index]
      properties:
        index:
          type: integer
          description: Index of the newly added channel.

    RemoveChannelParams:
      type: object
      required: [index]
      properties:
        index:
          type: integer
          description: Channel index to remove.

    SetDefaultChannelParams:
      type: object
      required: [index]
      properties:
        index:
          type: integer
          description: Channel index to set as default.

    SetMailboxParams:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean
          description: Whether to enable mailbox mode.

    SetLocationConfigParams:
      type: object
      properties:
        enabled:
          type: boolean
          description: Enable or disable location sharing.
        interval_s:
          type: integer
          description: Default update interval in seconds.
        default_tier:
          type: string
          description: Default location sharing tier.
        lat:
          type: number
          description: Optional manual latitude.
        lon:
          type: number
          description: Optional manual longitude.

    SetLocationContactParams:
      type: object
      required: [address, tier]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.
        tier:
          type: string
          description: Location sharing tier.

    RemoveLocationContactParams:
      type: object
      required: [address]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.

    ShareLocationOnceParams:
      type: object
      required: [address]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.

    # ── Notification Payloads ────────────────────────────────────────────────

    OnAckPayload:
      type: object
      description: Delivery acknowledgment for a sent message.
      required: [packetId, status]
      properties:
        packetId:
          type: integer
          description: Packet ID from sendMessage response.
        status:
          type: string
          enum: [delivered, failed]
          description: Delivery status.
        relayPath:
          type: array
          items:
            $ref: '#/components/schemas/RelayHop'
          description: Relay path taken (empty on failure).

    ProbeAckPayload:
      type: object
      description: A single probe response from a peer.
      required: [responderAddr, hopCount, rssi, snr, pathLen, receivedAt]
      properties:
        responderAddr:
          type: integer
          description: Address of the responding node.
        hopCount:
          type: integer
          description: Number of hops.
        rssi:
          type: integer
          description: RSSI of the response (dBm).
        snr:
          type: number
          description: SNR of the response (dB).
        pathLen:
          type: integer
          description: Path length.
        relayPath:
          type: array
          items:
            type: integer
          description: Relay addresses in path.
        receivedAt:
          type: integer
          description: Unix timestamp (ms) when received.

    ProbeCompletePayload:
      type: object
      description: Sent when probe window closes.
      required: [probeId]
      properties:
        probeId:
          type: integer
          description: Probe identifier.

    LocationUpdatePayload:
      type: object
      description: A location update notification.
      required: [addr, name, tier, online, lastUpdatedMs]
      properties:
        addr:
          type: integer
          description: Peer address.
        name:
          type: string
          description: Peer display name.
        tier:
          type: string
          enum: [full, coarse, presence]
        position:
          oneOf:
            - $ref: '#/components/schemas/Position'
            - type: 'null'
        online:
          type: boolean
        lastUpdatedMs:
          type: integer

# ── Notifications (extension) ──────────────────────────────────────────────

x-notifications:
  bramble.onMessage:
    description: >
      Incoming message received from the mesh. Delivered as a JSON-RPC
      notification (no id field).
    payload:
      $ref: '#/components/schemas/Message'

  bramble.onAck:
    description: >
      Delivery acknowledgment for a previously sent message. Matches
      the packetId returned by bramble.sendMessage.
    payload:
      $ref: '#/components/schemas/OnAckPayload'

  bramble.onNeighborChange:
    description: >
      Neighbor table has changed. Client should re-fetch via
      bramble.getNeighbors for updated data.
    payload:
      $ref: '#/components/schemas/EmptyParams'

  bramble.onRouteChange:
    description: >
      Routing table has changed. Client should re-fetch via
      bramble.getRoutes for updated data.
    payload:
      $ref: '#/components/schemas/EmptyParams'

  bramble.onProbeResult:
    description: >
      Probe response received from a peer node. Multiple notifications
      may arrive for a single probe, followed by probe.complete.
    payload:
      $ref: '#/components/schemas/ProbeAckPayload'

  probe.ack:
    description: Individual probe response from a peer.
    payload:
      $ref: '#/components/schemas/ProbeAckPayload'

  probe.complete:
    description: Probe window has closed; no more responses expected.
    payload:
      $ref: '#/components/schemas/ProbeCompletePayload'

  location.update:
    description: Location update received from a peer.
    payload:
      $ref: '#/components/schemas/LocationUpdatePayload'
