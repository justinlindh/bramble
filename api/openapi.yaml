openapi: 3.1.0
info:
  title: Bramble Mesh Node API
  description: >
    JSON-RPC 2.0 API for Bramble mesh nodes.
    Methods are invoked via JSON-RPC over serial (UART), WebSocket, or BLE.
    Each method is mapped to a POST path for codegen and documentation purposes.
    The actual wire format is standard JSON-RPC 2.0.
  version: 0.5.0
  license:
    name: MIT
    identifier: MIT

servers:
  - url: https://bramble.local
    description: Bramble node HTTP host (WebSocket JSON-RPC upgrades on /ws)

security: []

paths:
  # ── Query Methods ──────────────────────────────────────────────────────────

  /rpc/bramble.getStatus:
    post:
      operationId: getStatus
      summary: Get node status
      description: >
        Returns current node status including uptime, packet counters,
        neighbor/route counts, airtime usage, and GPS position.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Node status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponse'

  /rpc/bramble.getIdentity:
    post:
      operationId: getIdentity
      summary: Get node identity
      description: Returns the node's address and public key hash.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Node identity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResponse'

  /rpc/bramble.getVersion:
    post:
      operationId: getVersion
      summary: Get firmware version info
      description: Returns firmware version, protocol version, and hardware identifier.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Version info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VersionResponse'

  /rpc/bramble.getDeliveryEvents:
    post:
      operationId: getDeliveryEvents
      summary: Get delivery replay events since a sequence number
      description: >
        Returns persisted delivery events for ACK and broadcast-delivery telemetry.
        Use `sinceEventSeq` (or `since_event_seq`) for incremental replay.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                sinceEventSeq:
                  type: integer
                  minimum: 0
                since_event_seq:
                  type: integer
                  minimum: 0
                limit:
                  type: integer
                  minimum: 1
                  maximum: 1024
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Delivery replay window
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryEventsResponse'

  /rpc/bramble.getNeighbors:
    post:
      operationId: getNeighbors
      summary: Get direct radio neighbors
      description: Returns list of nodes heard directly via radio with signal quality metrics.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Neighbor list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NeighborsResponse'

  /rpc/bramble.getRoutes:
    post:
      operationId: getRoutes
      summary: Get routing table
      description: Returns all known routes including direct and multi-hop paths.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Route table
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoutesResponse'

  /rpc/bramble.getMessages:
    post:
      operationId: getMessages
      summary: Get stored messages
      description: Returns messages in the node's message store.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Message list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessagesResponse'

  /rpc/bramble.getAirtime:
    post:
      operationId: getAirtime
      summary: Get airtime budget status
      description: Returns per-tier airtime budget usage and remaining allocation.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Airtime stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AirtimeResponse'

  /rpc/bramble.getPeerLocations:
    post:
      operationId: getPeerLocations
      summary: Get peer location data
      description: Returns location information for all known peers, varying by sharing tier.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Peer locations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeerLocationsResponse'

  /rpc/bramble.ping:
    post:
      operationId: ping
      summary: Ping the node
      description: Health check. Returns pong with node address and protocol version.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Pong response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PingResponse'

  # ── Config / Action Methods ────────────────────────────────────────────────

  /rpc/bramble.getConfig:
    post:
      operationId: getConfig
      summary: Get full node configuration
      description: Returns identity, radio settings, channels, mailbox, and location config.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Full configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigResponse'

  /rpc/bramble.sendMessage:
    post:
      operationId: sendMessage
      summary: Send a direct text message
      description: >
        Sends a unicast message to a destination address.
        Returns immediately with a packet identifier; delivery status arrives
        via bramble.onAck notification.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMessageParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Message queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendMessageResponse'

  /rpc/bramble.sendBroadcast:
    post:
      operationId: sendBroadcast
      summary: Broadcast a text message
      description: Sends a broadcast message on the public channel.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendBroadcastParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Broadcast queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendBroadcastResponse'

  /rpc/bramble.sendProbe:
    post:
      operationId: sendProbe
      summary: Send a network probe
      description: >
        Broadcasts a probe packet. Responses arrive asynchronously as
        probe.ack notifications, followed by a probe.complete notification.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Probe initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendProbeResponse'

  /rpc/bramble.setRadio:
    post:
      operationId: setRadio
      summary: Update radio parameters
      description: Sets one or more radio parameters (frequency, spreading factor, bandwidth, TX power).
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetRadioParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Radio updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setNodeName:
    post:
      operationId: setNodeName
      summary: Set the node display name
      description: Sets the node name (max 32 characters).
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetNodeNameParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Name updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.addChannel:
    post:
      operationId: addChannel
      summary: Add a channel
      description: Adds a new channel with the given name and pre-shared key.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddChannelParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Channel added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddChannelResponse'

  /rpc/bramble.removeChannel:
    post:
      operationId: removeChannel
      summary: Remove a channel
      description: Removes a channel by index.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoveChannelParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Channel removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setDefaultChannel:
    post:
      operationId: setDefaultChannel
      summary: Set the default channel
      description: Sets which channel is used by default for outgoing messages.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetDefaultChannelParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Default channel updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setMailbox:
    post:
      operationId: setMailbox
      summary: Enable or disable mailbox mode
      description: Toggles store-and-forward mailbox functionality.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetMailboxParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Mailbox setting updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /rpc/bramble.setLocationConfig:
    post:
      operationId: setLocationConfig
      summary: Configure location sharing
      description: Enables/disables location sharing and sets default parameters.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetLocationConfigParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Location config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetLocationConfigResponse'

  /rpc/bramble.setLocationContact:
    post:
      operationId: setLocationContact
      summary: Add or update a location contact
      description: Sets the sharing tier and parameters for a specific peer address.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetLocationContactParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Contact updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetLocationContactResponse'

  /rpc/bramble.removeLocationContact:
    post:
      operationId: removeLocationContact
      summary: Remove a location contact
      description: Stops sharing location with the specified peer.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoveLocationContactParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Contact removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemoveLocationContactResponse'

  /rpc/bramble.shareLocationOnce:
    post:
      operationId: shareLocationOnce
      summary: Share location once with a peer
      description: Sends a one-time location update to the specified address.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShareLocationOnceParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Location shared
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShareLocationOnceResponse'

  /rpc/bramble.reboot:
    post:
      operationId: reboot
      summary: Reboot the node
      description: Triggers a software reboot of the node.
      tags: [action]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Reboot initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  # ── Adaptive Airtime Policy Methods ────────────────────────────────────────

  /rpc/bramble.getAirtimePolicy:
    post:
      operationId: getAirtimePolicy
      summary: Get adaptive airtime policy configuration and status
      description: >
        Returns the current adaptive airtime policy configuration, runtime status,
        and historical metrics. The policy dynamically adjusts beacon intervals
        based on mesh density, churn, and congestion conditions.
      tags: [query]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Airtime policy configuration and status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAirtimePolicyResponse'

  /rpc/bramble.setAirtimePolicy:
    post:
      operationId: setAirtimePolicy
      summary: Configure adaptive airtime policy parameters
      description: >
        Updates adaptive airtime policy configuration. Settings are persisted
        to NVS and take effect immediately. All parameters are optional.
      tags: [config]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetAirtimePolicyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Policy configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  # ── Traffic Debug Methods ──────────────────────────────────────────────────

  /rpc/bramble.setTrafficDebug:
    post:
      operationId: setTrafficDebug
      summary: Configure traffic debug telemetry
      description: >
        Enable or disable traffic debug event recording and configure sampling parameters.
        Settings are persisted to NVS and survive reboots.
      tags: [debug]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetTrafficDebugParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Traffic debug config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetTrafficDebugResponse'

  /rpc/bramble.getTrafficDebug:
    post:
      operationId: getTrafficDebug
      summary: Get traffic debug configuration and buffer state
      description: >
        Returns current traffic debug configuration, ring buffer utilization,
        and dropped event count.
      tags: [debug]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Traffic debug status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetTrafficDebugResponse'

  /rpc/bramble.getTrafficEvents:
    post:
      operationId: getTrafficEvents
      summary: Retrieve traffic events from ring buffer
      description: >
        Fetch traffic events from the in-memory ring buffer.
        Use since_seq to request only new events (incremental pull).
        Events are returned in chronological order (oldest first).
      tags: [debug]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetTrafficEventsParams'
      responses:
        '400':
          description: Bad request (invalid params or request body)
        '200':
          description: Traffic events
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetTrafficEventsResponse'

components:
  schemas:
    # ── Primitives / Empty ───────────────────────────────────────────────────

    EmptyParams:
      type: object
      description: No parameters required.
      properties: {}

    OkResponse:
      type: object
      description: Generic success response.
      required: [ok]
      properties:
        ok:
          type: boolean
          description: Always true on success.

    # ── Reusable Types ───────────────────────────────────────────────────────

    Position:
      type: object
      description: GPS position with accuracy and motion data.
      required: [lat, lon, alt, accuracy, timestampMs]
      properties:
        lat:
          type: number
          description: Latitude in decimal degrees.
        lon:
          type: number
          description: Longitude in decimal degrees.
        alt:
          type: number
          description: Altitude in meters.
        accuracy:
          type: number
          description: Horizontal accuracy in meters.
        speed:
          type: number
          description: Speed in meters per second.
        heading:
          type: number
          description: Heading in degrees (0-360).
        timestampMs:
          type: integer
          description: Position fix timestamp (Unix ms).

    Neighbor:
      type: object
      description: A directly heard radio neighbor.
      required: [addr, rssi, snr, lastHeardMs]
      properties:
        addr:
          type: integer
          description: Neighbor address (uint32).
        rssi:
          type: integer
          description: Received signal strength (dBm).
        snr:
          type: number
          description: Signal-to-noise ratio (dB).
        deliveryRate:
          type: integer
          description: Link delivery rate metric (0-255).
        lastHeardMs:
          type: integer
          description: Milliseconds since last packet received.
        isMailbox:
          type: boolean
          description: Whether this neighbor acts as a mailbox.
        airtimeRemaining:
          type: integer
          description: Estimated airtime remaining (percentage).

    Route:
      type: object
      description: A routing table entry.
      required: [dest, nextHop, hopCount, metric, state, lastUsedMs]
      properties:
        dest:
          type: integer
          description: Destination address (uint32).
        nextHop:
          type: integer
          description: Next-hop address (uint32).
        hopCount:
          type: integer
          description: Number of hops to destination.
        metric:
          type: integer
          description: Route cost metric (lower is better).
        state:
          type: string
          enum: [active, stale, discovering]
          description: Route state.
        lastUsedMs:
          type: integer
          description: Milliseconds since route was last used.

    Channel:
      type: object
      description: A configured channel.
      required: [id, name, hasPsk, is_default]
      properties:
        id:
          type: integer
          description: Channel index.
        name:
          type: string
          description: Channel display name.
        hasPsk:
          type: boolean
          description: Whether a pre-shared key is configured.
        epoch:
          type: integer
          description: Key epoch counter.
        is_default:
          type: boolean
          description: Whether this is the default channel.

    Message:
      type: object
      description: A stored or incoming message.
      required: [from, to, text, timestamp]
      properties:
        from:
          type: integer
          description: Sender address (uint32).
        to:
          type: integer
          description: Destination address (uint32, 0xFFFFFFFF for broadcast).
        text:
          type: string
          description: Message text content.
        tier:
          type: string
          enum: [critical, normal]
          description: Message priority tier.
        timestamp:
          type: integer
          description: Message timestamp (Unix seconds).
        msgId:
          type: string
          description: Unique message identifier.


    AirtimePolicyMode:
      type: string
      enum: [disabled, stable, dense, churn]
      description: >
        Adaptive airtime policy mode:
        - disabled: Policy inactive, using fixed beacon interval
        - stable: Normal operation with baseline beacon interval
        - dense: High neighbor count detected, increased beacon spacing
        - churn: Neighbor churn detected, temporary fast beaconing

    AirtimePolicyConfig:
      type: object
      description: Adaptive airtime policy configuration parameters.
      required: [enabled, baseIntervalMs, minIntervalMs, maxIntervalMs, denseThreshold, churnWindowSec, cooldownSec]
      properties:
        enabled:
          type: boolean
          description: Whether the adaptive policy is enabled.
        baseIntervalMs:
          type: integer
          description: Baseline beacon interval in milliseconds (stable mode).
        minIntervalMs:
          type: integer
          description: Minimum allowed beacon interval (churn mode).
        maxIntervalMs:
          type: integer
          description: Maximum allowed beacon interval (dense mode).
        denseThreshold:
          type: integer
          description: Neighbor count threshold to trigger dense mode.
        churnWindowSec:
          type: integer
          description: Time window (seconds) for detecting churn events.
        churnThreshold:
          type: integer
          description: Number of join/leave events to trigger churn mode.
        cooldownSec:
          type: integer
          description: Minimum time (seconds) between mode transitions (hysteresis).

    AirtimePolicyStatus:
      type: object
      description: Current adaptive airtime policy runtime status.
      required: [mode, currentIntervalMs, neighborCount, neighborDelta, churnEvents, lastTransitionMs]
      properties:
        mode:
          $ref: '#/components/schemas/AirtimePolicyMode'
        currentIntervalMs:
          type: integer
          description: Currently active beacon interval in milliseconds.
        neighborCount:
          type: integer
          description: Current neighbor count.
        neighborDelta:
          type: integer
          description: Change in neighbor count since last evaluation.
        churnEvents:
          type: integer
          description: Number of churn events in the current window.
        lastTransitionMs:
          type: integer
          description: Unix timestamp (ms) of last mode transition.
        congestionScore:
          type: integer
          description: Optional congestion score (0-100, future feature).

    LocationTier:
      type: string
      enum: [full, coarse, presence, off]
      description: Location sharing tier.

    LocationSource:
      type: string
      enum: [gps, manual, hybrid]
      description: Source mode for location reporting.

    LocationContactRule:
      type: object
      description: Per-peer location sharing rule.
      required: [address]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.
        enabled:
          type: boolean
          description: Whether rule is enabled.
        tier:
          $ref: '#/components/schemas/LocationTier'
        interval_s:
          type: integer
          description: Override update interval in seconds.

    LocationChannelTarget:
      type: object
      description: Per-channel location sharing rule.
      required: [channel]
      properties:
        channel:
          type: integer
          description: Channel index.
        enabled:
          type: boolean
          description: Whether rule is enabled.
        tier:
          $ref: '#/components/schemas/LocationTier'
        interval_s:
          type: integer
          description: Override update interval in seconds.

    LocationConfig:
      type: object
      description: Canonical location configuration.
      required: [enabled, tier, default_tier, interval_s, source, contact_rules, channel_targets]
      properties:
        enabled:
          type: boolean
          description: Whether location sharing is enabled.
        tier:
          $ref: '#/components/schemas/LocationTier'
        default_tier:
          $ref: '#/components/schemas/LocationTier'
        interval_s:
          type: integer
          description: Default update interval in seconds.
        source:
          $ref: '#/components/schemas/LocationSource'
        contact_rules:
          type: array
          items:
            $ref: '#/components/schemas/LocationContactRule'
        channel_targets:
          type: array
          items:
            $ref: '#/components/schemas/LocationChannelTarget'

    LocationPeer:
      type: object
      description: Location data for a peer node.
      required: [addr, name, tier, position, online, lastUpdatedMs]
      properties:
        addr:
          type: string
          description: Peer address as 8-char uppercase hex.
        name:
          type: string
          description: Peer display name.
        tier:
          $ref: '#/components/schemas/LocationTier'
        position:
          oneOf:
            - $ref: '#/components/schemas/Position'
            - type: 'null'
          description: Position data for this peer (null when unavailable).
        online:
          type: boolean
          description: Whether the peer is currently online.
        lastUpdatedMs:
          type: integer
          description: Unix timestamp (ms) of last update.

    # ── Response Schemas ─────────────────────────────────────────────────────

    StatusResponse:
      type: object
      description: Node status information.
      required: [uptimeSec, freeHeapBytes, fwVersion, txCount, rxCount, droppedCount, neighborCount, routeCount, airtimeUsedMs]
      properties:
        uptimeSec:
          type: integer
          description: Seconds since boot.
        freeHeapBytes:
          type: integer
          description: Free heap memory in bytes.
        fwVersion:
          type: string
          description: Firmware version string.
        txCount:
          type: integer
          description: Total packets transmitted.
        rxCount:
          type: integer
          description: Total packets received.
        droppedCount:
          type: integer
          description: Total packets dropped.
        neighborCount:
          type: integer
          description: Number of direct neighbors.
        routeCount:
          type: integer
          description: Number of active routes.
        airtimeUsedMs:
          type: integer
          description: Total airtime used in milliseconds.
        position:
          $ref: '#/components/schemas/Position'

    IdentityResponse:
      type: object
      description: Node identity information.
      required: [address, pubkey_hash]
      properties:
        address:
          type: string
          description: Node address as hex string (e.g. "4A555354").
        pubkey_hash:
          type: string
          description: Public key hash as hex string.

    VersionResponse:
      type: object
      description: Firmware version information.
      required: [firmware_version, protocol_version, hardware]
      properties:
        firmware_version:
          type: string
          description: Firmware version string.
        protocol_version:
          type: string
          description: Protocol version string.
        hardware:
          type: string
          description: Hardware platform identifier.
        supports_delivery_event_sync:
          type: boolean
          description: True when bramble.getDeliveryEvents incremental replay is supported.

    PingResponse:
      type: object
      description: Ping/pong response.
      required: [pong, address, protocol_version]
      properties:
        pong:
          type: boolean
          description: Always true.
        address:
          type: string
          description: Node address as hex string.
        protocol_version:
          type: string
          description: Protocol version string.

    DeliveryEvent:
      type: object
      required: [event_seq, event_type, timestamp_ms, payload]
      properties:
        event_seq:
          type: integer
        event_id:
          type: string
        event_type:
          type: string
          enum: [ack, broadcast_delivery]
        packet_id:
          type: string
          description: Packet id for ACK delivery events.
        broadcast_id:
          type: string
          description: Broadcast id for broadcast delivery events.
        timestamp_ms:
          type: integer
        payload:
          type: object
          additionalProperties: true

    DeliveryEventsResponse:
      type: object
      required: [events, latest_event_seq]
      properties:
        events:
          type: array
          items:
            $ref: '#/components/schemas/DeliveryEvent'
        latest_event_seq:
          type: integer

    NeighborsResponse:
      type: object
      required: [neighbors]
      properties:
        neighbors:
          type: array
          items:
            $ref: '#/components/schemas/Neighbor'

    RoutesResponse:
      type: object
      required: [routes]
      properties:
        routes:
          type: array
          items:
            $ref: '#/components/schemas/Route'

    MessagesResponse:
      type: object
      required: [messages]
      properties:
        messages:
          type: array
          items:
            $ref: '#/components/schemas/Message'

    AirtimeResponse:
      type: object
      description: Current airtime budget state (flat firmware format).
      required:
        - critical_remaining_ms
        - normal_remaining_ms
        - broadcast_remaining_ms
        - critical_max_ms
        - normal_max_ms
        - broadcast_max_ms
        - next_refill_ms
      properties:
        critical_remaining_ms:
          type: integer
        normal_remaining_ms:
          type: integer
        broadcast_remaining_ms:
          type: integer
        critical_max_ms:
          type: integer
        normal_max_ms:
          type: integer
        broadcast_max_ms:
          type: integer
        next_refill_ms:
          type: integer

    GetAirtimePolicyResponse:
      type: object
      description: Adaptive airtime policy configuration and runtime status.
      required: [config, status]
      properties:
        config:
          $ref: '#/components/schemas/AirtimePolicyConfig'
        status:
          $ref: '#/components/schemas/AirtimePolicyStatus'

    PeerLocationsResponse:
      type: object
      required: [peerLocations]
      properties:
        peerLocations:
          type: array
          items:
            $ref: '#/components/schemas/LocationPeer'

    ConfigResponse:
      type: object
      description: Full node configuration.
      required: [identity, radio, channels, mailboxEnabled, location]
      properties:
        identity:
          type: object
          required: [address, pubkeyHash, name, pubkeyB64]
          properties:
            address:
              type: integer
              description: Node address (uint32).
            pubkeyHash:
              type: integer
              description: Public key hash (uint32).
            name:
              type: string
              description: Node display name.
            pubkeyB64:
              type: string
              description: Base64-encoded public key.
        radio:
          type: object
          required: [txPowerDbm, sf, bwKhz, cr, freqMhz]
          properties:
            txPowerDbm:
              type: integer
              description: Transmit power in dBm.
            sf:
              type: integer
              description: LoRa spreading factor (7-12).
            bwKhz:
              type: integer
              description: Bandwidth in kHz (125, 250, or 500).
            cr:
              type: integer
              description: Coding rate (5-8).
            freqMhz:
              type: number
              description: Frequency in MHz.
        channels:
          type: array
          items:
            $ref: '#/components/schemas/Channel'
        mailboxEnabled:
          type: boolean
          description: Whether store-and-forward mailbox is enabled.
        location:
          $ref: '#/components/schemas/LocationConfig'

    # ── Request Params ───────────────────────────────────────────────────────

    SendMessageParams:
      type: object
      description: Parameters for sending a direct message.
      required: [dest, text]
      properties:
        dest:
          type: string
          description: Destination node address as 8-char uppercase hex (e.g. "04CAAAF8").
        text:
          type: string
          description: Message text content.

    SendMessageResponse:
      type: object
      required: [packetId, status, fragmented, max_bytes, actual_bytes]
      properties:
        packetId:
          type: string
          description: Packet ID as uppercase hex string.
        status:
          type: string
          enum: [sent]
        fragmented:
          type: boolean
          description: Whether this message will be sent as multiple fragments.
        fragments_total:
          type: integer
          description: Total number of fragments (only present if fragmented=true).
        max_bytes:
          type: integer
          description: Maximum message size in bytes (616 with fragmentation).
        actual_bytes:
          type: integer
          description: Actual message size in bytes.

    SendBroadcastParams:
      type: object
      required: [text]
      properties:
        text:
          type: string

    SendBroadcastResponse:
      type: object
      description: Response fields currently emitted by firmware for bramble.sendBroadcast.
      required: [broadcast_id, status, broadcast, channel, fragmented, max_bytes, actual_bytes]
      properties:
        broadcast_id:
          type: string
          description: Broadcast identifier as uppercase hex string.
        status:
          type: string
          enum: [sent]
        broadcast:
          type: boolean
        channel:
          type: integer
        fragmented:
          type: boolean
          description: Whether this message will be sent as multiple fragments.
        fragments_total:
          type: integer
          description: Total number of fragments (only present if fragmented=true).
        max_bytes:
          type: integer
          description: Maximum message size in bytes (616 with fragmentation).
        actual_bytes:
          type: integer
          description: Actual message size in bytes.

    SendProbeResponse:
      type: object
      required: [probeId, ackWindow]
      properties:
        probeId:
          type: integer
          description: Probe identifier.
        ackWindow:
          type: integer
          description: Seconds to wait for probe responses.

    SetRadioParams:
      type: object
      description: Radio parameters to update. All fields optional.
      properties:
        tx_power_dbm:
          type: integer
          description: Transmit power in dBm.
        sf:
          type: integer
          description: LoRa spreading factor (6-12).
        bw_hz:
          type: integer
          description: Bandwidth in Hz (125000, 250000, 500000).
        coding_rate:
          type: integer
          description: Coding rate denominator (5-8).
        frequency_mhz:
          type: number
          description: Frequency in MHz.

    SetNodeNameParams:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Node display name (max 32 characters).
          maxLength: 32

    AddChannelParams:
      type: object
      required: [name, psk]
      properties:
        name:
          type: string
          description: Channel display name.
        psk:
          type: string
          description: Pre-shared key (base64 or hex encoded).

    AddChannelResponse:
      type: object
      required: [index]
      properties:
        index:
          type: integer
          description: Index of the newly added channel.

    RemoveChannelParams:
      type: object
      required: [index]
      properties:
        index:
          type: integer
          description: Channel index to remove.

    SetDefaultChannelParams:
      type: object
      required: [index]
      properties:
        index:
          type: integer
          description: Channel index to set as default.

    SetMailboxParams:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean
          description: Whether to enable mailbox mode.

    SetLocationConfigParams:
      type: object
      description: Canonical location policy update. All fields optional for partial updates.
      properties:
        enabled:
          type: boolean
          description: Enable or disable location sharing.
        tier:
          $ref: '#/components/schemas/LocationTier'
        default_tier:
          $ref: '#/components/schemas/LocationTier'
        interval_s:
          type: integer
          description: Default update interval in seconds.
        source:
          $ref: '#/components/schemas/LocationSource'
        contact_rules:
          type: array
          items:
            $ref: '#/components/schemas/LocationContactRule'
        channel_targets:
          type: array
          items:
            $ref: '#/components/schemas/LocationChannelTarget'

    SetLocationConfigResponse:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
          description: True when the config update is accepted.

    SetLocationContactParams:
      type: object
      required: [address]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.
        enabled:
          type: boolean
          description: Whether this contact rule is enabled.
        tier:
          $ref: '#/components/schemas/LocationTier'
        interval_s:
          type: integer
          description: Contact-specific update interval in seconds.

    SetLocationContactResponse:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
          description: True when the contact update is accepted.

    RemoveLocationContactParams:
      type: object
      required: [address]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.

    RemoveLocationContactResponse:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
          description: True when the contact removal is accepted.

    ShareLocationOnceParams:
      type: object
      required: [address]
      properties:
        address:
          type: string
          description: Peer address as 8-char uppercase hex.

    ShareLocationOnceResponse:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
          description: True when one-time share request is accepted.

    SetAirtimePolicyParams:
      type: object
      description: Adaptive airtime policy configuration parameters. All fields optional.
      properties:
        enabled:
          type: boolean
          description: Whether to enable the adaptive policy.
        baseIntervalMs:
          type: integer
          description: Baseline beacon interval in milliseconds (stable mode).
        minIntervalMs:
          type: integer
          description: Minimum allowed beacon interval (churn mode).
        maxIntervalMs:
          type: integer
          description: Maximum allowed beacon interval (dense mode).
        denseThreshold:
          type: integer
          description: Neighbor count threshold to trigger dense mode.
        churnWindowSec:
          type: integer
          description: Time window (seconds) for detecting churn events.
        churnThreshold:
          type: integer
          description: Number of join/leave events to trigger churn mode.
        cooldownSec:
          type: integer
          description: Minimum time (seconds) between mode transitions (hysteresis).

    BroadcastDeliveryNotification:
      type: object
      description: Delivery telemetry notification for a previously queued broadcast packet.
      required: [recipient, broadcast_id, status, rssi_at_dest]
      properties:
        recipient:
          type: string
          description: Recipient node address as 8-char uppercase hex.
        broadcast_id:
          type: string
          description: Broadcast identifier returned by bramble.sendBroadcast.
        status:
          type: string
          description: Delivery state for this telemetry event.
          enum: [delivered]
        rssi_at_dest:
          type: integer
          description: RSSI measured at recipient, in dBm.
        relayPath:
          type: array
          description: Optional sampled relay path when telemetry mode includes path sampling.
          items:
            type: object
            required: [addr]
            properties:
              addr:
                type: string
                description: Relay node address as 8-char uppercase hex.

    # ── Traffic Debug / Monitor Stream Types ──────────────────────────────

    SetTrafficDebugParams:
      $ref: '#/components/schemas/TrafficDebugConfig'

    TrafficDebugConfig:
      type: object
      description: Canonical traffic debug configuration used by monitor/event-stream APIs.
      properties:
        enabled:
          type: boolean
          description: Enable or disable traffic event recording.
          default: false
        include_tx:
          type: boolean
          description: Record TX (transmit) events.
          default: true
        include_rx:
          type: boolean
          description: Record RX (receive) events.
          default: true
        sample_rate:
          type: integer
          description: >
            Sampling rate percentage (0-100). 100 = capture all events,
            50 = capture ~50% of events (randomized sampling).
          minimum: 0
          maximum: 100
          default: 100

    TrafficDebugStatus:
      allOf:
        - $ref: '#/components/schemas/TrafficDebugConfig'
        - type: object
          required: [enabled, include_tx, include_rx, sample_rate, buffer_capacity, buffer_count, dropped_count]
          properties:
            buffer_capacity:
              type: integer
              description: Ring buffer capacity (max events that can be stored).
            buffer_count:
              type: integer
              description: Number of events currently in buffer.
            dropped_count:
              type: integer
              description: Total events dropped due to buffer wrap or backpressure.

    SetTrafficDebugResponse:
      allOf:
        - type: object
          description: Response from bramble.setTrafficDebug.
          required: [ok]
          properties:
            ok:
              type: boolean
              description: Always true on success.
        - $ref: '#/components/schemas/TrafficDebugConfig'

    GetTrafficDebugResponse:
      allOf:
        - type: object
          description: Response from bramble.getTrafficDebug.
        - $ref: '#/components/schemas/TrafficDebugStatus'

    GetTrafficEventsParams:
      type: object
      description: Parameters for bramble.getTrafficEvents.
      properties:
        since_seq:
          type: integer
          description: >
            Only return events with seq > since_seq. Use 0 or omit to fetch all available events.
          default: 0
        limit:
          type: integer
          description: Maximum number of events to return (1-512).
          minimum: 1
          maximum: 512
          default: 100

    GetTrafficEventsResponse:
      type: object
      description: Response from bramble.getTrafficEvents.
      required: [events, returned, total_available]
      properties:
        events:
          type: array
          description: Array of traffic events (oldest first).
          items:
            $ref: '#/components/schemas/TrafficEvent'
        returned:
          type: integer
          description: Number of events returned in this response.
        total_available:
          type: integer
          description: Total events currently available in buffer.

    TrafficEvent:
      type: object
      description: >
        A single traffic event capturing TX or RX packet metadata.
        Used for airtime analysis and network efficiency monitoring.
      required: [seq, timestamp_ms, pkt_type, category, airtime_tier, packet_len, rssi, is_tx]
      properties:
        seq:
          type: integer
          description: Monotonic sequence number (unique per event).
        timestamp_ms:
          type: integer
          description: Event timestamp in milliseconds since boot.
        pkt_type:
          type: integer
          description: Packet type ID from firmware packet.h.
        category:
          type: string
          description: Classified traffic category.
          enum: [beacon, timesync, routing, ack, chat, maintenance, other, unknown]
        airtime_tier:
          type: string
          description: Airtime bucket used for this packet.
          enum: [none, normal, critical, broadcast, unknown]
        packet_len:
          type: integer
          description: Packet length in bytes (on-air payload size).
        rssi:
          type: integer
          description: Received signal strength in dBm (0 for TX events).
        is_tx:
          type: boolean
          description: true if this is a TX event, false for RX.

    MonitorEventStreamNotification:
      type: object
      description: Canonical JSON-RPC notification envelope for monitor/event-stream events.
      required: [jsonrpc, method, params]
      properties:
        jsonrpc:
          type: string
          enum: ['2.0']
        method:
          $ref: '#/components/schemas/MonitorEventMethod'
        params:
          oneOf:
            - $ref: '#/components/schemas/BroadcastDeliveryNotification'
            - $ref: '#/components/schemas/TrafficEvent'
            - $ref: '#/components/schemas/WifiEvent'
            - $ref: '#/components/schemas/GpsEvent'
            - $ref: '#/components/schemas/LocationEvent'
          description: Notification payload for the emitted monitor event.

    MonitorEventMethod:
      type: string
      description: Canonical monitor/event-stream notification method names.
      enum: [bramble.onBroadcastDelivery, bramble.onTrafficEvent, bramble.onWifiEvent, bramble.onGpsEvent, bramble.onLocationEvent]

    WifiEvent:
      type: object
      required: [event, mode, connected, ip]
      properties:
        event:
          type: string
          enum: [connected, disconnected, ip_changed]
        mode:
          type: string
          enum: [off, sta, ap]
        connected:
          type: boolean
        ssid:
          type: string
        ip:
          type: string
        rssi:
          type: integer

    GpsEvent:
      type: object
      required: [event]
      properties:
        event:
          type: string
          enum: [fix_acquired, fix_lost]
        valid:
          type: boolean
        lat:
          type: number
        lon:
          type: number
        alt_m:
          type: integer
        sats:
          type: integer

    LocationEvent:
      type: object
      required: [event, tier, timestamp_ms]
      properties:
        event:
          type: string
          enum: [sent, received]
        peer:
          type: string
          description: Hex peer address for received events.
        tier:
          type: integer
        timestamp_ms:
          type: integer
        rssi:
          type: integer
        snr:
          type: integer
        count:
          type: integer
          description: Number of recipients in a location send cycle.

# ── Notifications (extension) ──────────────────────────────────────────────

x-notifications:
  bramble.onMessage:
    description: >
      Incoming message received from the mesh. Delivered as a JSON-RPC
      notification (no id field).
    payload:
      $ref: '#/components/schemas/Message'

  bramble.onAck:
    description: >
      Delivery acknowledgment for a previously sent message. Matches
      the packetId returned by bramble.sendMessage.
    payload:
      $ref: '#/components/schemas/OnAckPayload'

  bramble.onBroadcastDelivery:
    description: >
      Broadcast delivery telemetry event emitted after bramble.sendBroadcast.
      Correlate using both broadcastId and packetId from SendBroadcastResponse.
      One broadcast may produce multiple delivery notifications.
    payload:
      $ref: '#/components/schemas/BroadcastDeliveryNotification'
    envelope:
      $ref: '#/components/schemas/MonitorEventStreamNotification'

  bramble.onNeighborChange:
    description: >
      Neighbor table has changed. Client should re-fetch via
      bramble.getNeighbors for updated data.
    payload:
      $ref: '#/components/schemas/EmptyParams'

  bramble.onRouteChange:
    description: >
      Routing table has changed. Client should re-fetch via
      bramble.getRoutes for updated data.
    payload:
      $ref: '#/components/schemas/EmptyParams'

  bramble.onProbeResult:
    description: >
      Probe response received from a peer node. Multiple notifications
      may arrive for a single probe, followed by probe.complete.
    payload:
      $ref: '#/components/schemas/ProbeAckPayload'

  probe.ack:
    description: Individual probe response from a peer.
    payload:
      $ref: '#/components/schemas/ProbeAckPayload'

  probe.complete:
    description: Probe window has closed; no more responses expected.
    payload:
      $ref: '#/components/schemas/ProbeCompletePayload'

  location.update:
    description: Location update received from a peer.
    payload:
      $ref: '#/components/schemas/LocationUpdatePayload'

  bramble.onTrafficEvent:
    description: >
      Real-time traffic event notification. Emitted when traffic debug is enabled
      and a new TX or RX event is recorded. Clients can use this for live monitoring
      or reconnect backfill via bramble.getTrafficEvents(since_seq).
    payload:
      $ref: '#/components/schemas/TrafficEvent'
    envelope:
      $ref: '#/components/schemas/MonitorEventStreamNotification'

  bramble.onWifiEvent:
    description: WiFi state transition event (connect/disconnect/IP change).
    payload:
      $ref: '#/components/schemas/WifiEvent'
    envelope:
      $ref: '#/components/schemas/MonitorEventStreamNotification'

  bramble.onGpsEvent:
    description: GPS fix transition event.
    payload:
      $ref: '#/components/schemas/GpsEvent'
    envelope:
      $ref: '#/components/schemas/MonitorEventStreamNotification'

  bramble.onLocationEvent:
    description: Location sharing send/receive event.
    payload:
      $ref: '#/components/schemas/LocationEvent'
    envelope:
      $ref: '#/components/schemas/MonitorEventStreamNotification'
