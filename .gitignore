# ESP-IDF build artifacts
build/
build-*/
test/build/

# Release factory images (built in CI, attached to firmware releases)
/dist/

# Toolchain/object outputs
*.o
*.obj
*.a
*.d
*.elf
*.map
*.bin

# Local SDK config/state
sdkconfig
sdkconfig.*
!sdkconfig.defaults*
sdkconfig.old
!test/stubs/sdkconfig.h

# Dependency/build management
managed_components/
dependencies.lock

# Temp/evidence artifacts
tmp-e2e/
# libFuzzer crash/timeout/OOM reproducers; CI uploads them as job artifacts
# instead, and the committed seeds live in test/fuzz/corpus/.
test/fuzz/artifacts/
*.log
.cache/

# Python cache
__pycache__/
*.pyc

# Simulator artifacts
simulator/gosim/bramble-gosim

# webapp artifacts
webapp/node_modules/
webapp/dist/
webapp/.vite/
webapp/e2e-screenshots/
webapp/public/web-flasher/
webapp/mock/node_modules/

# internal-only doc trees (not published; see MEMORY/plans workflow instead)
docs/archive/
docs/superpowers/
docs/plans/

# Local helper state
.flash-targets.json
reports/
webapp/node_modules

# OTA signing keys: private key must never be committed
keys/*
!keys/README.md
!keys/ota-release-pub.pem

# session scratch (never commit)
.superp
.superpowers/
.claude/worktrees/

# KiCad editor state and lock files (per-machine, never shared)
*.kicad_prl
*.lck
~*.lck

# Internal AI-assistant ops docs, kept local/private (not shipped public)
/.claude/
